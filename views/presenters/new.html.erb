<% title "New Presenter" %>

<%= form_for @presenter, :url => { :action => :create }, :html => { :multipart => true } do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %> 
<% end %>

<p><%= link_to "Back to List", event_presenters_path %></p>
