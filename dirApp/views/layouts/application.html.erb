<!DOCTYPE html>
<html>
  <head>
    <link rel="shortcut icon" href="/favicon.ico" />
    <title><%= content_for?(:title) ? yield(:title) : get_name_or_logo %></title>
    <%= stylesheet_link_tag "application" %>
    <%= render 'layouts/stylesheets' %>
    <%= stylesheet_link_tag "custom", "menu", "screen",'jquery-ui-1.8.11.custom','jquery.fancybox-1.3.4', :cache=> true %>
    <% unless user_signed_in? %>
      <%= stylesheet_link_tag "user" %>
    <% end %>
    <%= javascript_include_tag :defaults, 'jquery-1.6.2.min', 'jquery-ui-1.8.11.custom.min', 'rails.validations', 'jquery.infieldlabel.min', 'jquery-ui-timepicker-addon', 'fancybox/jquery.fancybox-1.3.4.pack', :cache => true %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>
  <body>
    <div id="container">
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
      <%= content_tag :h1, yield(:title) if show_title? %>
      <div id="spinner" style="display:none"></div>
      <%= yield %>
      <%= yield :content %>
      <%= yield :footer %>
    </div>
  </body>
</html>
