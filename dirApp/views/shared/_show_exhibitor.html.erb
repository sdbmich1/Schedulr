    <% [['name', 'Name:'],['description', 'Description:'], ['location', 'Location:']].each do |fld| %>
   	<% unless @exhibitor.send(fld[0]).blank? %>	
  	  <div class="span-2"><%= fld[1] %></div>
 	  <div class="<%= sbody + ' last' %>"><%= @exhibitor.send(fld[0]) %></div>
 	<% end %>
    <% end %>
