
    <%= f.fields_for :event_tracks do |builder| %>  
      <%= render :partial => 'track', :locals => {:builder => builder} %>  
    <% end %> 

    <%= f.fields_for :event_sites do |builder| %>  
      <%= render :partial => 'site', :locals => {:builder => builder} %>  
    <% end %> 
